node_js:
- "4"
- "6"
- "8"
- "10"
- "12"
- "stable"
sudo: false
language: node_js
script:
  - "npm run test-cov"
  - "if [ $TRAVIS_NODE_VERSION == stable ]; then npx standard; fi"
after_script: "npm install coveralls@2 && cat ./coverage/lcov.info | coveralls"
